[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: integration tests requiring full app/db wiring
    slow: slower tests that may be excluded for quick local loops
